# 🍳 תוסף מסך מטבח - Kitchen Display System (KDS)

## 📋 סקירה כללית

תוסף מסך מטבח מאפשר לעסקים להציג הזמנות בזמן אמת על מסך ייעודי - מושלם למסעדות, בתי קפה, מאפיות, ובתי מלאכה. ההזמנות מוצגות ככרטיסים עם כל הפרטים הנדרשים, שעון סופר, והתראות קוליות.

### 🎯 מטרות
1. **תצוגה ברורה** - הזמנות מוצגות ככרטיסים גדולים וקריאים
2. **זמן אמת** - polling כל דקה (לא מכביד על השרת)
3. **התראות** - צליל פעמון כשנכנסת הזמנה חדשה
4. **מעקב זמן** - שעון סופר מרגע יצירת ההזמנה
5. **ניהול זרימה** - כפתורי אישור/ביטול/הדפסה

### 💰 עלות
**89₪ לחודש** (14 ימי ניסיון חינם)

---

## 🏗️ ארכיטקטורה

### שימוש בתשתית קיימת
- ✅ טבלת `orders` קיימת
- ✅ טבלת `order_items` קיימת
- ✅ `customOrderStatuses` בחנות
- ✅ הגדרות בטבלת `store_plugins.config`

### מה חדש
- 📺 מסך תצוגה ייעודי (fullscreen)
- 🔔 מערכת התראות קוליות
- ⏱️ שעון סופר בכל כרטיס
- 🖨️ כפתור הדפסה מהיר

---

## 📁 מבנה קבצים

```
src/
├── app/
│   └── shops/
│       └── [slug]/
│           └── admin/
│               └── plugins/
│                   └── kitchen-display/
│                       ├── page.tsx              # דף הגדרות
│                       ├── display/
│                       │   └── page.tsx          # מסך התצוגה (fullscreen)
│                       ├── kitchen-display.tsx   # קומפוננטת תצוגה (client)
│                       ├── order-card.tsx        # כרטיס הזמנה
│                       ├── timer.tsx             # שעון סופר
│                       ├── sound-alert.tsx       # התראה קולית
│                       └── actions.ts            # Server Actions
```

---

## 💻 עיצוב UI

### Layout - מסך התצוגה

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 🍳 מסך מטבח                           🔔 התראות: פעיל    ⛶ מסך מלא    ⚙️    │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐              │
│  │ 🟢 הזמנה #1042  │  │ 🟡 הזמנה #1041  │  │ 🔴 הזמנה #1040  │              │
│  │ ⏱️ 00:02:34     │  │ ⏱️ 00:08:12     │  │ ⏱️ 00:15:47     │              │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤              │
│  │ 📱 050-1234567  │  │ 📱 052-9876543  │  │ 📱 054-1111111  │              │
│  │ 👤 ישראל ישראלי │  │ 👤 דנה כהן      │  │ 👤 משה לוי      │              │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤              │
│  │                 │  │                 │  │                 │              │
│  │ 🍕 פיצה גדולה  │  │ 🍔 המבורגר     │  │ ☕ קפה הפוך     │              │
│  │    x1   ₪49    │  │    x2   ₪78    │  │    x3   ₪42    │              │
│  │    + זיתים     │  │    + ללא בצל   │  │    + שקדים     │              │
│  │    + פטריות    │  │                 │  │                 │              │
│  │                 │  │ 🍟 צ'יפס       │  │ 🥐 קרואסון     │              │
│  │                 │  │    x2   ₪24    │  │    x2   ₪28    │              │
│  │                 │  │                 │  │                 │              │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤              │
│  │ 📝 הערות:      │  │ 📝 הערות:      │  │                 │              │
│  │ בלי גלוטן!     │  │ -              │  │                 │              │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤              │
│  │ סה״כ: ₪49      │  │ סה״כ: ₪102     │  │ סה״כ: ₪70      │              │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤              │
│  │ ✅ אישור  ❌ ביטול │  │ ✅ אישור  ❌ ביטול │  │ ✅ אישור  ❌ ביטול │              │
│  │      🖨️ הדפסה  │  │      🖨️ הדפסה  │  │      🖨️ הדפסה  │              │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘              │
│                                                                              │
│  ┌─────────────────┐  ┌─────────────────┐                                   │
│  │ 🟢 הזמנה #1039  │  │ 🟢 הזמנה #1038  │                                   │
│  │ ⏱️ 00:01:15     │  │ ⏱️ 00:03:22     │                                   │
│  │ ...             │  │ ...             │                                   │
│  └─────────────────┘  └─────────────────┘                                   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### צבעי שעון לפי זמן המתנה
- 🟢 **ירוק** - עד 5 דקות
- 🟡 **צהוב** - 5-10 דקות
- 🔴 **אדום** - מעל 10 דקות

### עקרונות עיצוב
- **כהה/בהיר** - תמיכה במצב כהה (מומלץ למסכי מטבח)
- **RTL מלא** - תמיכה בעברית
- **Responsive** - תצוגה טובה בכל גודל מסך
- **High Contrast** - צבעים בולטים לקריאות טובה
- **גדול וברור** - טקסט גדול לקריאה ממרחק

---

## 🔧 פרטי יישום

### 1. הגדרת התוסף (Registry)

```typescript
// src/lib/plugins/registry.ts
{
  slug: 'kitchen-display',
  name: 'מסך מטבח',
  description: 'הצג הזמנות בזמן אמת על מסך ייעודי עם התראות קוליות, שעון סופר וכפתורי ניהול',
  type: 'core',
  category: 'operations',
  version: '1.0.0',
  icon: 'monitor',
  author: 'QuickShop',
  isFree: false,
  price: 89,
  trialDays: 14,
  defaultConfig: {
    enabled: true,
    // סטטוסים להצגה (מה מוצג על המסך)
    displayStatuses: ['pending', 'confirmed', 'processing'],
    // סטטוס אחרי לחיצה על "אישור"
    successStatus: 'processing', // או customStatus ID
    successCustomStatus: null,   // אם רוצים סטטוס מותאם אישית
    // סטטוס אחרי לחיצה על "ביטול"
    cancelStatus: 'cancelled',
    cancelCustomStatus: null,
    // הגדרות תצוגה
    soundEnabled: true,
    soundVolume: 80, // 0-100
    refreshInterval: 60, // שניות
    sortOrder: 'oldest_first', // 'oldest_first' | 'newest_first'
    showCustomerPhone: true,
    showCustomerName: true,
    showOrderNotes: true,
    showProductImages: true,
    cardSize: 'medium', // 'small' | 'medium' | 'large'
    darkMode: true,
    // זמנים לצבעי התראה (בדקות)
    warningTimeMinutes: 5,
    dangerTimeMinutes: 10,
  },
  metadata: {
    menuItem: {
      icon: 'Monitor',
      label: 'מסך מטבח',
      href: '/plugins/kitchen-display',
      section: 'addons',
      badge: 'חדש',
    },
    features: [
      'תצוגת הזמנות בזמן אמת',
      'התראות קוליות',
      'שעון סופר לכל הזמנה',
      'כפתורי אישור/ביטול/הדפסה',
      'סטטוסים מותאמים אישית',
      'מצב מסך מלא',
      'תמיכה במצב כהה',
      'תמונות מוצרים ותוספות',
    ],
    screenshots: [],
  },
}
```

### 2. ממשק הגדרות התוסף

```typescript
// types.ts
interface KitchenDisplayConfig {
  enabled: boolean;
  
  // אילו סטטוסים להציג על המסך
  displayStatuses: ('pending' | 'confirmed' | 'processing' | 'shipped')[];
  
  // מה קורה בלחיצה על "אישור"
  successStatus: 'confirmed' | 'processing' | 'shipped' | 'delivered';
  successCustomStatus: string | null; // ID של customOrderStatus
  
  // מה קורה בלחיצה על "ביטול"  
  cancelStatus: 'cancelled';
  cancelCustomStatus: string | null;
  
  // הגדרות צליל
  soundEnabled: boolean;
  soundVolume: number; // 0-100
  
  // רענון
  refreshInterval: number; // שניות (מינימום 30)
  
  // מיון
  sortOrder: 'oldest_first' | 'newest_first';
  
  // תצוגה
  showCustomerPhone: boolean;
  showCustomerName: boolean;
  showOrderNotes: boolean;
  showProductImages: boolean;
  cardSize: 'small' | 'medium' | 'large';
  darkMode: boolean;
  
  // זמני התראה
  warningTimeMinutes: number;
  dangerTimeMinutes: number;
}
```

### 3. שליפת הזמנות

```typescript
// actions.ts
'use server';

export async function getKitchenOrders(
  storeId: string,
  config: KitchenDisplayConfig
): Promise<KitchenOrder[]> {
  // שליפת הזמנות לפי הסטטוסים שהוגדרו
  const orders = await db.query.orders.findMany({
    where: and(
      eq(orders.storeId, storeId),
      inArray(orders.status, config.displayStatuses),
      // רק הזמנות ששולמו (או לפי הגדרה)
      eq(orders.financialStatus, 'paid')
    ),
    orderBy: config.sortOrder === 'oldest_first' 
      ? asc(orders.createdAt) 
      : desc(orders.createdAt),
    with: {
      orderItems: {
        with: {
          product: {
            columns: { name: true },
            with: {
              images: {
                where: eq(productImages.isPrimary, true),
                limit: 1,
              }
            }
          },
          variant: {
            columns: { optionValues: true }
          }
        }
      }
    }
  });
  
  return orders.map(order => ({
    id: order.id,
    orderNumber: order.orderNumber,
    customerName: order.customerName,
    customerPhone: order.customerPhone,
    status: order.status,
    customStatus: order.customStatus,
    total: order.total,
    note: order.note,
    createdAt: order.createdAt,
    items: order.orderItems.map(item => ({
      id: item.id,
      name: item.name,
      quantity: item.quantity,
      price: item.price,
      imageUrl: item.product?.images?.[0]?.url,
      variantTitle: item.variantTitle,
      // תוספות/הערות מה-metadata
      addons: item.metadata?.addons || [],
      notes: item.metadata?.notes || '',
    })),
  }));
}

export async function updateOrderStatus(
  orderId: string,
  storeId: string,
  newStatus: string,
  customStatus?: string
): Promise<{ success: boolean; error?: string }> {
  try {
    await db.update(orders)
      .set({
        status: newStatus as any,
        customStatus: customStatus || null,
        updatedAt: new Date(),
      })
      .where(and(
        eq(orders.id, orderId),
        eq(orders.storeId, storeId)
      ));
    
    return { success: true };
  } catch (error) {
    return { success: false, error: 'שגיאה בעדכון הסטטוס' };
  }
}
```

### 4. קומפוננטת כרטיס הזמנה

```typescript
// order-card.tsx
interface OrderCardProps {
  order: KitchenOrder;
  config: KitchenDisplayConfig;
  onApprove: (orderId: string) => void;
  onCancel: (orderId: string) => void;
  onPrint: (orderId: string) => void;
}

export function OrderCard({ order, config, onApprove, onCancel, onPrint }: OrderCardProps) {
  const elapsedMinutes = Math.floor((Date.now() - new Date(order.createdAt).getTime()) / 60000);
  
  const timerColor = elapsedMinutes < config.warningTimeMinutes 
    ? 'text-green-500' 
    : elapsedMinutes < config.dangerTimeMinutes 
      ? 'text-yellow-500' 
      : 'text-red-500';
  
  return (
    <div className={`rounded-2xl border-2 overflow-hidden ${
      config.darkMode ? 'bg-slate-800 border-slate-700' : 'bg-white border-slate-200'
    }`}>
      {/* Header */}
      <div className={`px-4 py-3 border-b ${
        config.darkMode ? 'bg-slate-900 border-slate-700' : 'bg-slate-50 border-slate-200'
      }`}>
        <div className="flex items-center justify-between">
          <span className="text-lg font-bold">#{order.orderNumber}</span>
          <Timer createdAt={order.createdAt} className={timerColor} />
        </div>
      </div>
      
      {/* Customer Info */}
      {(config.showCustomerName || config.showCustomerPhone) && (
        <div className="px-4 py-2 border-b border-slate-200">
          {config.showCustomerName && order.customerName && (
            <p className="font-medium">👤 {order.customerName}</p>
          )}
          {config.showCustomerPhone && order.customerPhone && (
            <p className="text-slate-500">📱 {order.customerPhone}</p>
          )}
        </div>
      )}
      
      {/* Items */}
      <div className="p-4 space-y-3">
        {order.items.map(item => (
          <div key={item.id} className="flex gap-3">
            {config.showProductImages && item.imageUrl && (
              <img src={item.imageUrl} className="w-12 h-12 rounded-lg object-cover" />
            )}
            <div className="flex-1">
              <div className="flex items-start justify-between">
                <span className="font-medium">{item.name}</span>
                <span className="text-slate-500">x{item.quantity}</span>
              </div>
              {item.variantTitle && (
                <p className="text-sm text-slate-400">{item.variantTitle}</p>
              )}
              {item.addons?.length > 0 && (
                <div className="mt-1 space-y-0.5">
                  {item.addons.map((addon, i) => (
                    <p key={i} className="text-sm text-blue-400">+ {addon}</p>
                  ))}
                </div>
              )}
            </div>
          </div>
        ))}
      </div>
      
      {/* Notes */}
      {config.showOrderNotes && order.note && (
        <div className="px-4 py-2 bg-yellow-50 border-t border-yellow-100">
          <p className="text-sm text-yellow-800">📝 {order.note}</p>
        </div>
      )}
      
      {/* Total */}
      <div className="px-4 py-2 border-t border-slate-200">
        <p className="text-lg font-bold text-left">₪{order.total}</p>
      </div>
      
      {/* Actions */}
      <div className="p-3 border-t border-slate-200 flex gap-2">
        <button
          onClick={() => onApprove(order.id)}
          className="flex-1 py-2 bg-green-500 hover:bg-green-600 text-white rounded-xl font-medium"
        >
          ✅ אישור
        </button>
        <button
          onClick={() => onCancel(order.id)}
          className="flex-1 py-2 bg-red-500 hover:bg-red-600 text-white rounded-xl font-medium"
        >
          ❌ ביטול
        </button>
        <button
          onClick={() => onPrint(order.id)}
          className="p-2 bg-slate-200 hover:bg-slate-300 rounded-xl"
        >
          🖨️
        </button>
      </div>
    </div>
  );
}
```

### 5. שעון סופר

```typescript
// timer.tsx
'use client';

import { useEffect, useState } from 'react';

interface TimerProps {
  createdAt: Date | string;
  className?: string;
}

export function Timer({ createdAt, className }: TimerProps) {
  const [elapsed, setElapsed] = useState('00:00:00');
  
  useEffect(() => {
    const startTime = new Date(createdAt).getTime();
    
    const updateTimer = () => {
      const now = Date.now();
      const diff = Math.floor((now - startTime) / 1000);
      
      const hours = Math.floor(diff / 3600);
      const minutes = Math.floor((diff % 3600) / 60);
      const seconds = diff % 60;
      
      setElapsed(
        `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`
      );
    };
    
    updateTimer();
    const interval = setInterval(updateTimer, 1000);
    
    return () => clearInterval(interval);
  }, [createdAt]);
  
  return (
    <span className={`font-mono text-lg font-bold ${className}`}>
      ⏱️ {elapsed}
    </span>
  );
}
```

### 6. התראות קוליות

```typescript
// sound-alert.tsx
'use client';

import { useEffect, useRef } from 'react';

interface SoundAlertProps {
  enabled: boolean;
  volume: number;
  newOrderIds: string[];
  lastPlayedIds: Set<string>;
  onPlayed: (ids: string[]) => void;
}

export function SoundAlert({ enabled, volume, newOrderIds, lastPlayedIds, onPlayed }: SoundAlertProps) {
  const audioRef = useRef<HTMLAudioElement | null>(null);
  
  useEffect(() => {
    if (!enabled) return;
    
    // מצא הזמנות חדשות שעוד לא השמענו עליהן
    const unplayedIds = newOrderIds.filter(id => !lastPlayedIds.has(id));
    
    if (unplayedIds.length > 0 && audioRef.current) {
      audioRef.current.volume = volume / 100;
      audioRef.current.play().catch(console.error);
      onPlayed(unplayedIds);
    }
  }, [enabled, volume, newOrderIds, lastPlayedIds, onPlayed]);
  
  return (
    <audio ref={audioRef} preload="auto">
      {/* צליל פעמון - נשתמש בקובץ קיים או נייצר */}
      <source src="/sounds/bell-notification.mp3" type="audio/mpeg" />
      <source src="/sounds/bell-notification.ogg" type="audio/ogg" />
    </audio>
  );
}
```

---

## ⚡ אופטימיזציות ביצועים

### 1. Polling חכם
- רענון כל 60 שניות (ברירת מחדל)
- ניתן להגדיר בין 30-120 שניות
- **לא WebSocket** - כדי לא להכביד

### 2. Server Components
- דף הגדרות - Server Component
- מסך תצוגה - Client Component (לטיימרים וסאונד)

### 3. Optimistic UI
```typescript
// בלחיצה על אישור/ביטול:
1. הכרטיס מיד נעלם מהמסך (optimistic)
2. API call ברקע
3. אם נכשל - הכרטיס חוזר עם הודעת שגיאה
```

### 4. מניעת רינדורים מיותרים
```typescript
// React.memo על OrderCard
// useMemo על רשימת ההזמנות
// useCallback על handlers
```

---

## 🔄 זרימת תהליך

```
┌─────────────────┐
│ הזמנה חדשה     │ (מהאתר / POS / API)
└────────┬────────┘
         ↓
┌─────────────────────────────────────────┐
│ ההזמנה נשמרת ב-DB עם status: pending   │
└────────────────────┬────────────────────┘
                     ↓
┌─────────────────────────────────────────┐
│        מסך המטבח מרענן (polling)        │
│  → מזהה הזמנה חדשה                      │
│  → מנגן צליל פעמון 🔔                   │
│  → מציג כרטיס חדש                       │
└────────────────────┬────────────────────┘
                     ↓
┌─────────────────────────────────────────┐
│       עובד המטבח מכין את ההזמנה        │
│  (שעון סופר רץ בזמן אמת)               │
└────────────────────┬────────────────────┘
                     ↓
      ┌──────────────┴───────────────┐
      ↓                              ↓
┌───────────┐                ┌───────────────┐
│ ✅ אישור  │                │ ❌ ביטול      │
│           │                │               │
│ → שינוי   │                │ → שינוי       │
│   סטטוס   │                │   סטטוס       │
│   ל״מוכן״ │                │   ל״מבוטל״    │
└───────────┘                └───────────────┘
      ↓                              ↓
┌─────────────────────────────────────────┐
│    הכרטיס נעלם ממסך המטבח              │
└─────────────────────────────────────────┘
```

---

## 📄 דף הגדרות

### תצוגה

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  🍳 מסך מטבח - הגדרות                                           │
│                                                                  │
│  ═══════════════════════════════════════════════════════════     │
│                                                                  │
│  📺 פתח מסך תצוגה                    [→ פתח במסך מלא]           │
│                                                                  │
│  ─────────────────────────────────────────────────────────────   │
│                                                                  │
│  ⚙️ הגדרות תצוגה                                                │
│                                                                  │
│  סטטוסים להצגה:                                                 │
│  ☑ ממתין    ☑ אושר    ☑ בהכנה    ☐ נשלח                        │
│                                                                  │
│  מיון הזמנות:                                                   │
│  [● ישן לחדש]  [○ חדש לישן]                                     │
│                                                                  │
│  גודל כרטיסים:                                                  │
│  [○ קטן]  [● בינוני]  [○ גדול]                                  │
│                                                                  │
│  ─────────────────────────────────────────────────────────────   │
│                                                                  │
│  🔔 התראות קוליות                                               │
│                                                                  │
│  התראות פעילות: [████████░░] 80%                                │
│  🔊 בדוק צליל                                                   │
│                                                                  │
│  ─────────────────────────────────────────────────────────────   │
│                                                                  │
│  📊 פעולות                                                       │
│                                                                  │
│  סטטוס אחרי "אישור":                                            │
│  [▼ בהכנה (processing)              ]                           │
│  או סטטוס מותאם:                                                │
│  [▼ בחר סטטוס מותאם...              ]                           │
│                                                                  │
│  סטטוס אחרי "ביטול":                                            │
│  [▼ מבוטל (cancelled)               ]                           │
│                                                                  │
│  ─────────────────────────────────────────────────────────────   │
│                                                                  │
│  🎨 עיצוב                                                        │
│                                                                  │
│  ☑ מצב כהה (מומלץ למטבח)                                        │
│  ☑ הצג תמונות מוצרים                                            │
│  ☑ הצג שם לקוח                                                  │
│  ☑ הצג טלפון לקוח                                               │
│  ☑ הצג הערות להזמנה                                             │
│                                                                  │
│  ─────────────────────────────────────────────────────────────   │
│                                                                  │
│  ⏱️ זמני התראה                                                   │
│                                                                  │
│  צהוב (אזהרה) אחרי: [5] דקות                                    │
│  אדום (דחוף) אחרי: [10] דקות                                    │
│                                                                  │
│  ─────────────────────────────────────────────────────────────   │
│                                                                  │
│                                        [💾 שמור הגדרות]          │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🖨️ הדפסה

### פורמט הדפסה (תרמית 58/80mm)

```
================================
        🏪 שם החנות
================================

הזמנה: #1042
תאריך: 28/01/2026 14:32

--------------------------------
👤 ישראל ישראלי
📱 050-1234567
--------------------------------

פריטים:
--------------------------------
🍕 פיצה גדולה           x1
   + זיתים
   + פטריות
                        ₪49.00

🥤 קולה                 x2
                        ₪12.00
--------------------------------

סה״כ:               ₪61.00

--------------------------------
📝 הערות:
בלי גלוטן!
================================
```

---

## ✅ רשימת פיצ'רים מלאה

### Phase 1 - MVP
- [x] הגדרת תוסף ב-Registry
- [x] דף הגדרות בסיסי
- [x] מסך תצוגה fullscreen
- [x] כרטיסי הזמנות
- [x] שעון סופר
- [x] כפתורי אישור/ביטול
- [x] Polling כל דקה

### Phase 2 - שיפורים
- [x] התראות קוליות
- [x] צבעים לפי זמן המתנה
- [x] תמיכה בסטטוסים מותאמים
- [x] כפתור הדפסה
- [x] מצב כהה

### Phase 3 - Polish
- [ ] סטטיסטיקות (זמן ממוצע להכנה)
- [ ] סינון לפי קטגוריה/מקור
- [ ] ריבוי תחנות (כל תחנה רואה פריטים אחרים)
- [ ] התראות push (אופציונלי)

---

## 🔐 הרשאות

- נדרשת גישה לאדמין של החנות
- לא נגיש מהפרונט
- אפשר לפתוח על מסך נפרד (טאבלט/מסך מטבח)

---

## 📱 תמיכה במכשירים

- ✅ מסך מחשב (מומלץ)
- ✅ טאבלט
- ✅ מובייל (תצוגה מצומצמת)
- ✅ מסכי מגע

---

## ✅ Checklist לפני השקה

- [ ] תוסף מותקן ופעיל
- [ ] הגדרות סטטוסים נבדקו
- [ ] צליל התראה נבדק
- [ ] בדיקת שינוי סטטוס
- [ ] בדיקת הדפסה
- [ ] בדיקה על מסך מגע
- [ ] בדיקת מצב כהה

